# TEST_REPORT.md — состояние тестов

**Ветка:** `ai/improvements`. **Прогон:** `pnpm test` (vitest, headless, без DOM/Pixi).

## Итог

| Пакет | Файлов | Тестов | Статус |
|---|---:|---:|---|
| core | 28 | **243** | ✅ |
| app | 26 | 99 | ✅ |
| render | 8 | 42 | ✅ |
| input | 2 | 13 | ✅ |
| shared | 0 | 0 | — |
| **Всего** | **64** | **397** | ✅ всё зелёное |

**Было на старте:** 357 тестов. **Стало:** 397 (**+40**). typecheck чистый, lint зелёный (на старте был красный), `pnpm build` собирается.

Падающих тестов на старте не было — поэтому «чинить падающие» не потребовалось; вся работа — это *новое* покрытие на дыры из аудита + защита от регресса моих правок.

## Что добавлено (по фазам)

### Детерминизм / реплей (ядро инварианта §8) — НОВЫЙ ФАЙЛ
`packages/core/src/world/determinism.test.ts` (4):
- **Байт-в-байт:** боевой прогон 700 тиков из одного `(seed, [commands])` даёт идентичное финальное состояние во двух запусках (сравнение глубокое, включая `Map`-ы AI-состояний). Это и есть проверка «забег воспроизводим из (seed, команды)», которую просил Сергей.
- Другой `seed` → расходится (RNG реально рулит симуляцией).
- Тот же seed, но **другие команды** → расходится (забег = seed + команды).
- `tick` — чистый: не мутирует вход, возвращает новое состояние.
- **Бонус:** этот тест охраняет мои рефакторы (`explosionEvents` пер-тик, hoist `nextEntityId`, гард `range`) от любого скрытого слома детерминизма — все три прошли байт-в-байт.

### Физика (Фаза 2)
`plane-physics.test.ts` (+5): потолочная/половая сила (нос вниз у потолка / вверх у земли + softFloor), кламп `ceilingProximity ≤1`, X-обёртка устойчива к смещению >1 ширины мира, NaN-гард множителей скорости/форсажа.

### ИИ — анти-самоубийство (Фаза 3)
`chase-policy.test.ts` (+3): спасительный слой-1 (тот самый класс «враги врезаются в землю») — вытягивание носа у самой земли (огонь подавляется), ранний выход из пикирования при «низко+круто вниз», разрыв столкновения ВВЕРХ когда есть высота. Раньше эта логика была без прямого покрытия.

### «Забег» / билд-крафт (Фазы 2-3)
- `run-waves.test.ts` (+5): 15 волн, эскорт-волна 15 = 0 врагов, выход за диапазон → 0, полная таблица, HP-множитель монотонен и клампится.
- `branches.test.ts` (+7): `keystoneTier` на границах 3/6/9, никогда не отрицателен; маппинг веток.
- `run-summary.test.ts` (+2): фракционный дебриф Шакалов (названия/метки/«Ведомый» вместо сырого `wingman`); дефолт = С.О.В.
- `upgrade-pool.test.ts` (+1): эволюция без пререквизитов «фейлит закрыто» (не бросает в ролле).
- `apply-upgrade.test.ts` (+2): сигнатурный `wingman` (hasDrone + накопление droneCount).

### Оружие / RNG / ввод (Фазы 2-3)
- `weapon-system.test.ts` (+5): веер мультишота (разные id, симметричные смещения), тяжёлая пушка (пробитие/кулдаун).
- `mulberry32.test.ts` (+1): гард `range(min,max)` при `max≤min` (и сохранение потока RNG).
- `tick.test.ts` (+1 проверка): `explosionEvents` сбрасывается пер-тик (регресс утечки).
- `keyboard.test.ts` (НОВЫЙ, +5): чистый маппинг клавиш (взаимоисключения A/D, W/S, Space=fire+jump, B=bomb+special) + пустой набор = нейтраль (состояние после blur).

## Заметки
- Превью игру не крутит (rAF замирает в фоне) — визуально не проверял; всё на headless-тестах + чистой сборке + ясном буте (как и предписано).
- Несколько тест-файлов писались параллельными агентами с само-проверкой (каждый прогонял свой файл до зелёного); финальный авторитет — общий `pnpm test` выше.
- Оставшиеся тест-дыры (диалоговый advance/done, kill-scoring по каждой причине смерти) — низкий приоритет, отмечены в `AUDIT.md`; не критичны, ядро уже под детерминизм-тестом.
