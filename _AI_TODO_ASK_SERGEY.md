# _AI_TODO_ASK_SERGEY — что я НЕ трогал (решай сам)

Это про **геймплей, продукт и архитектуру** — не баги. Я их НЕ менял (по правилам автономной задачи: высокий риск = тебе решать). Технику чинил сам (см. `AUDIT.md` / `_AI_WORKLOG.md`).

## ✅ РЕШЕНО в live-сессии (2026-06-23)
- **Враги-Шакалы при игре за С.О.В.** — Сергей выбрал «тяжёлые, но честные»; сделано (тяжёлая «бум»-пушка + лёгкий бафф HP/скорости, наложено на прокачку волн). Числа в `JACKAL_ENEMY_*`/`ARENA_JACKAL_ENEMY_*` — подкрутить по плейтесту.
- **Перегрев = мгновенный game-over** — был баг, починен (теперь всегда «загорелся → горишь → падаешь»).
- **Белый «карман» на спрайте С.О.В.** — вырезан.
- **Деплой** — всё выложено в прод по просьбе Сергея.

## Геймплей / ощущение (продуктовые)
1. **Лёгкий «слив высоты» на крейсере.** На полном газу самолёт постоянно получает крошечный сток вниз (сток включается ниже `G_MAX_LEVEL=680`, а устойчивая скорость на газу садится чуть ниже из-за драга; именованный порог «столла» `G_STALL=620` при этом в стороне). Не баг — вопрос **ощущения полёта**: должен ли биплан на крейсере мягко тонуть или держать высоту? Если «держать» — переключить онсет стока на `G_STALL`. `plane-physics.ts:157-162`. **Не трогал — это калибровка stall-фишки.**

## Архитектура / контент (твоё решение, риск регресса геймплея)
2. **`resolveRunFlow` (движок «Забега») протестирован, но НЕ подключён.** Живая прогрессия забега написана отдельным кодом в `main.ts`; чистая стейт-машина `run-flow.ts` (duel→pick→win/lose, «не побеждать по числу врагов на 15-й волне», одна жизнь) живёт только в тестах. Тесты дают ложную уверенность. Варианты: (а) подключить её как единый источник правды (риск — изменит поведение забега, нужно ре-плейтестить), (б) удалить как мёртвую. Парно: `run-state.advanceWave`/`RunState.wave` тоже только в тестах. **Не делал — меняет ход забега.**
3. **`spawn-system.ts` — мёртвый модуль.** Стохастический спавнер по времени, нигде не подключён (живые волны идут через `arena-waves`/run-логику). Это **заготовка под «Свободную охоту» §4** или мусор? Если режим планируется — оставить (и при подключении зажать `dt`, иначе риск масс-спавна/зависания). Если нет — в архив. **Жду твоего слова.**
4. **Иконки карточек билд-крафта** всё ещё векторные эмблемы веток + ватермарка (не индивидуальные иконки). Это арт-задача (Фаза 4) — статус и что нагенерил см. `_AI_WORKLOG.md`. Дизайн каждой иконки/что рисовать — продуктовое.
5. **Чистки мёртвого кода с большим объёмом** (рисованный fallback самолёта `body.ts`, отключённый декор неба `sky-background.ts ~300 строк`, `xpMagnetRange`-плумбинг). Технически безопасны, но объёмны и трогают визуальный/стейт-код — **оставил, чтобы не вносить шум вслепую**. Скажи «чисти» — вычищу отдельным коммитом.

## Из памяти/прежних заметок (не мои находки, но висят)
6. **«Ведомый» Фаза-2**: ведомый со своим HP, исчезает при сбитии, рисуется как мини-самолёт (сейчас — бессмертный `droneCount`). Геймплей/арт.
7. **Кредит CC-BY движку (jakobthiesen)** в игре перед релизом (звук). Юридическое обязательство.
8. **Музыкальная подложка боя** — нужен реальный аудио-ассет (продуктовое/ассет-решение).

---
_Технические баги/утечки/тесты, которые я УЖЕ починил — в `_AI_WORKLOG.md`. Откат — там же первой секцией._
