# _AI_WORKLOG — автономная работа Claude (Opus 4.8)

**Старт:** 2026-06-22. Ветка: `ai/improvements` (БЕЗ авто-деплоя). В `origin` НЕ пушится.

---

## ⏪ КОМАНДЫ ОТКАТА (для Сергея)

```bash
# 1. Вернуться к рабочему состоянию (твоя ветка с авто-деплоем):
git checkout feat/hud-animated-panel && git branch -D ai/improvements

# 2. Жёстко к точке до AI-правок (если что-то поломалось в любой ветке):
git reset --hard pre-ai-backup

# 3. Аварийно (если git вообще сломан): удалить папку Biplanes,
#    переименовать ..\Biplanes_BACKUP_2026-06-22 обратно в Biplanes,
#    выполнить: pnpm install
```

**Что гарантировано:**
- Тег `pre-ai-backup` указывает на коммит `465727b` (твоё состояние + 1 wip-коммит config-файлов).
- Физическая копия всей папки: `..\Biplanes_BACKUP_2026-06-22` (4.6 ГБ, включая `.git` и все ассеты; без `node_modules` — ставится `pnpm install`).
- Вся моя работа — ТОЛЬКО в локальной ветке `ai/improvements`. В `origin` ничего не ушло. Прод (gh-pages) НЕ затронут.

---

## ШАГ 0 — точка отката (СДЕЛАНО)
- [x] `git status` снят; ветка была `feat/hud-animated-panel`.
- [x] Незакоммиченное (`.gitignore`, `.claude/launch.json` — тривиальные config-правки) закоммичено как `wip before AI work` (`465727b`). Огромные сырые видео-папки в корне (`0607*`, `INTRO1`, `intro 2`, `SOV fraction/`, `Crimson Jackals Fraction/`, `Crimson victory/` — ~190 МБ) НЕ коммитил: память фиксирует, что они намеренно вне git (сжатые версии лежат в `apps/web/public`). Они сохранены физической копией.
- [x] Тег `pre-ai-backup` создан.
- [x] Физическая копия `..\Biplanes_BACKUP_2026-06-22` (robocopy, exit 1 = успех).
- [x] Ветка `ai/improvements` создана и активна.
- [x] Этот worklog заведён.

---

## ФАЗА 1 — АУДИТ ✅ → `AUDIT.md`
9 параллельных агентов прочли весь код по подсистемам → **45 находок**, приоритизированы, топ верифицированы чтением исходника. Геймплей/архитектура отделены в `_AI_TODO_ASK_SERGEY.md`.
**Базовая линия зафиксирована:** 357 тестов зелёные, typecheck чистый, сборка ОК, **lint красный (3 ошибки в core + 2 в app, всплыли при полном прогоне)**.

## ФАЗА 2 — ПОЧИНКА ✅ (технические баги/утечки, по коммитам)
- **🔴 Фриз-поверхность:** убран **живой `BlurFilter`** с занятого glow-FX-слоя (`glow-layer.ts`) — отдельный от убранного ранее masked-emblem, тот же класс краша мобильных GPU. Сильный кандидат на «всё замёрзло». Аддитивный блендинг сохранён. (`894f80a`)
- **Lint → зелёный:** убраны 5 мёртвых символов (`HP_REGEN_PER_SEC`/`worldHeight`/`GROUND_Y`/`drawRing` + zero-width пробел). Сырые видео-папки в корне (~190МБ) добавлены в `.gitignore` (чуть не утекли в коммит — поймал, откатил). (`894f80a`)
- **Утечка памяти:** `explosionEvents` рос безгранично весь забег (spread всей истории каждый тик) → пер-тиковый буфер + сбор по кадру в app (взрывы не теряются). (`8208b3a`)
- **Баг дебрифа Шакалов:** показывал сырой `wingman` и метки С.О.В. → фракционный `buildRunSummary`. (`b77a822`)
- **Физика-харднинг (детерминизм-safe):** modulo X-обёртка (plane+pilot), кламп `ceilingProximity`, NaN-гард множителей; гард `range()` RNG; «фейл-закрыто» эволюций; правка комментария мультишота. (`cdc6c79`)
- **Утечка GPU-геометрии:** спрайты врагов/пилотов теперь `destroy()` (не только removeChild; общий спрайт-шит не трогаем) + сброс stale one-shot VFX-детекторов в `resetRunState` + чистка `enemyHpForFx` O(n+m). (`2f429de`)
- **Залипание клавиш:** `blur`/`visibilitychange` → очистка набора (alt-tab больше не «зажимает» газ/руль). (`91cfdd0`)
- **Рендер-throw края:** портрет диалога (деление на 0 текстуры → Infinity), фолбэк ветки в `branch-style` (throw в рисовании level-up), сплайс death-фильтра (не затирает чужие), текст ошибки спрайт-шита. (`69be3b7`)
- **Перф:** hoist скана `nextEntityId` из цикла спавна (id байт-идентичны) + destroy у SupplyFx. (`112cb99`)
- НЕ трогал защищённые зоны (dt-кламп, парабола Шакалов, асимметрия фракций, ловец ошибок) — проверено.

## ФАЗА 3 — ТЕСТЫ ✅ → `TEST_REPORT.md`
**357 → 397 тестов** (+40), всё зелёное. Ключевое: **тест детерминизма/реплея** (700 тиков боя байт-в-байт из `(seed, commands)`) — и инвариант §8, и защита моих рефакторов. Плюс закрыт класс «ИИ врезается в землю», волны «Забега», `keystoneTier`, мультишот/тяжёлая пушка, `wingman`, маппинг клавиш. 4 тест-файла писались параллельными агентами с само-проверкой; финал — общий `pnpm test`.

## ФАЗА 4 — ГРАФИКА ✅ (сэмпл-набор)
Проверил пайплайн Codex (`image_generation`, stable). Сгенерил **стиль-проб из 4 иконок карточек** (по одной на ветку: Бронебойные ленты / Зажигательная смесь / «Ведомый» / Усиление корпуса), 512×512, прозрачный фон, дизельпанк-латунь — в НОВУЮ папку `apps/web/public/assets/run/cards_new/` (+ README). Старый арт не трогал. **НЕ подключал в UI** — полный набор ~25 иконок + проводка в level-up это продуктово-дизайнерская задача (стиль на твоё «ок», см. README папки + TODO). Красный шар Шакалов уже существовал. (`9429e62`)

## ФАЗА 5 — ПОЛИШ ✅
Низкорисковый технический/UX-полиш сделан по ходу Фазы 2 (чистка мёртвого кода, типизация/гарды, перф, обработка ошибок, доступность ввода). Объёмные чистки мёртвого кода и всё геймплейное — намеренно в `_AI_TODO_ASK_SERGEY.md`, не вслепую.

---

## ИТОГ
- **11 коммитов** в `ai/improvements` (локально, не в origin). Прод не затронут.
- 357 → **397 тестов**, lint красный→зелёный, typecheck чистый, `pnpm build` собирается.
- Главное: убрана вероятная причина фриза (живой BlurFilter), починены утечки памяти/GPU, фракционный баг дебрифа, харднинг физики/RNG — всё под защитой теста детерминизма (байт-в-байт реплей).
- Что отложено и почему — `_AI_TODO_ASK_SERGEY.md`. Полный разбор находок — `AUDIT.md`. Тесты — `TEST_REPORT.md`.
- Проверять: `git checkout ai/improvements && pnpm install && pnpm test && pnpm build`. Откат — секция вверху этого файла.
