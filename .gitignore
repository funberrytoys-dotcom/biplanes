node_modules/
.pnpm-store/
dist/
.DS_Store
*.log
.env
.env.local
tmp/
vendor/
.vite/
coverage/
.idea/
.vscode/
.claude/worktrees/

.superpowers/

# Raw video/audio SOURCE folders dropped in the repo root (compressed versions
# live under apps/web/public/assets, which ARE tracked). These are large (~190MB)
# and intentionally NOT committed — keep them out of `git add -A`.
/0607*/
/Crimson*/
/INTRO1/
/SOV fraction/
/intro 2/

# Capacitor native projects — generated in CI, not committed
apps/web/android/
apps/web/ios/
